---
---
<section id="newsletter" class="newsletter">
  <div class="newsletter-inner container">
    <div class="newsletter-text">
      <h2 class="newsletter-title">Receba os próximos artigos</h2>
      <p class="newsletter-desc">
        Conteúdo direto, sem enrolação. Uma vez por semana, na sua inbox.
      </p>
    </div>
    <form class="newsletter-form" action="#" method="POST">
      <input type="email" placeholder="Seu e-mail" required class="newsletter-input" />
      <button type="submit" class="newsletter-btn">Assinar</button>
    </form>
  </div>
</section>

<style>
  .newsletter {
    padding: 72px 0;
  }
  .newsletter-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }
  .newsletter-title {
    font-family: var(--sans);
    font-size: 26px;
    font-weight: 800;
    letter-spacing: -0.025em;
    color: var(--black);
  }
  .newsletter-desc {
    font-family: var(--serif);
    font-size: 16px;
    color: var(--text-med);
    margin-top: 8px;
    line-height: 1.6;
  }
  .newsletter-form {
    display: flex;
    gap: 12px;
  }
  .newsletter-input {
    flex: 1;
    border: 1.5px solid var(--line);
    border-radius: 8px;
    padding: 12px 16px;
    font-family: var(--sans);
    font-size: 14px;
    color: var(--text-body);
    outline: none;
    transition: border-color 0.2s;
  }
  .newsletter-input:focus { border-color: var(--black); }
  .newsletter-btn {
    background: var(--black);
    color: var(--white);
    border-radius: 8px;
    padding: 12px 24px;
    font-family: var(--sans);
    font-size: 14px;
    font-weight: 600;
    transition: background 0.2s;
  }
  .newsletter-btn:hover { background: var(--orange); }

  @media (max-width: 768px) {
    .newsletter-inner {
      grid-template-columns: 1fr;
      gap: 32px;
    }
    .newsletter-form { flex-direction: column; }
  }
</style>
